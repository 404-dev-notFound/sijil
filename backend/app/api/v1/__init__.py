# Phase 1+: auth.py, companies.py, shipments.py, classification.py, billing.py route
# handlers go here — thin controllers only (validate input, call a service, return the
# result). See architecture doc Sections 5 and 7 for the layering rules.
